# Frontend (Vite) — copy values to frontend/.env
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_VAPID_PUBLIC_KEY=

# Netlify function secrets (set in Netlify UI or CLI — never commit private key)
# VAPID_PUBLIC_KEY= same as VITE_VAPID_PUBLIC_KEY
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:hello@chatsnap.app
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
